# Phase 2 / V — 记牌器视觉重做（统一底色 + 矢量卡条 + 尺寸按比例 + 段头 / 折叠）

> **状态：🎨 D2 待确认（2026-09-15）**，未定稿不写代码。对照页：https://claude.ai/code/artifact/a323180a-6996-47eb-a753-53acab8fdf92
>
> **D1 → D2（用户 09-15 选定）**：B 酒馆方向，但费用格改回 A 的方块；饱和度压低；蓝（稀有）/ 紫（史诗）按色盲可分重选。
> 费用格色板：普通 `#5A5650` · 稀有 `#3B6B8F`（偏青钢蓝）· 史诗 `#9A5F8C`（偏洋红、明度高一档）· 传说 `#A8762C`（压暗琥珀，计数框仍 ★）。
> 页面里用 SVG `feColorMatrix` 做了红 / 绿 / 蓝色盲模拟。备选第二线索：费用格左缘细线（稀有一道、史诗两道），先不加，看实机。
> 定稿后拆两本任务书（V1 矢量卡条 + 尺寸；V2 段头 + 折叠 + 三行头压行高 + 不锁定拖拽吸附），本文件届时改成总纲。

## 为什么合成一片（2026-09-15 用户拍板）

Phase 2 / T1 实战（09-14）的三条视觉反馈指向同一处：

| # | 反馈 | 单独修的困难 |
|---|---|---|
| ⑧ | 三段 UI 字体 / 视觉与整体不符 | 段头没法和主题 PNG 卡条统一，PNG 各自带框 |
| ⑨ | 整体 overlay 太大 | PLAN 2.8：五档预设锁死 6.38 : 1、不随窗口缩放，前提是摆脱固定尺寸贴图 |
| ⑩ | 长卡名右侧被遮（公诉人梅尔特拉尼克斯） | 名字区宽度被贴图 `frameRect` 锁死 |

用户问「我们也自己弄个统一底色如何」→ 结论：可以，但它等于**卡条矢量化**（PLAN 2.8 末段早写着「矢量化归本节」），三条一起做比分三次补省。

## 已决

- 视觉语言按 Firestone：**整个面板一个底色**，三行头 / 段头 / 卡条同一行网格（≈ 21 px @1920×1080），段头居中带张数 + 折叠箭头，没有自己的方块。参考图 `docs/research/assets/firestone-zones-2026-09-15.png`，实测尺寸 `docs/research/firestone-overlay.md` 第七节。
- **四套主题 PNG 退役，只留一套外观**，主题选择器随之删；Phase 1 T1 的「与 `CardBar` 逐像素一致」作废。
- 先出设计稿比稿再写代码（T5 的流程）。
- 排在 Bug T6（数据）之后；与 T6 都动 `TrackerViewModel`，不并行。

## D1 两稿（对照页里有 2× / 1× 渲染，用的是仓库自带的 Belwe + 文鼎中隶和本地缓存的真实卡图）

| | A 石板 | B 酒馆 |
|---|---|---|
| 底色 | 冷灰蓝 `#151b27` × `trackerOpacity` | 暖木 `#1b1410` × `trackerOpacity` |
| 细线 | 白 14% | 金 `#d6b26e` 28% |
| 费用格 | 22×21 整格按稀有度着色（灰 / 蓝 / 紫 / 橙） | 16 px 圆宝石，稀有度即宝石色 |
| 段名 | 浅灰居中 | 金字居中 |

共同参数：面板 171 px（8.9% 窗口宽）、卡条 21 px（1.94% 窗口高）、宽高比 8.14 : 1 且压缩时宽度同步收；卡名区 ≈ 80 px 超长省略号截断、最小 9 px；原画右 100 px 用现有 `tiles/*.jpg`，渐隐 35 px 用底色；计数框 20 px，传说单张 ★；已打出段 45% 不透明（2.7 图标留位）；手牌段不再绿名。

## 等用户定

1. A / B / 组合。
2. ~~折叠手势：锁定态浮窗 `ignoresMouseEvents` 收不到点击~~ **已决（用户 09-15）：记牌器改成「不锁定 + 边缘吸附」**，折叠 / 拖拽 / 吸附共用一个鼠标模型，归 V2：
   - 窗口 `borderless + nonactivatingPanel`，`ignoresMouseEvents = false`，`isMovableByWindowBackground = true`；**不要**现在解锁那套带标题栏的 styleMask（`WindowManager.swift:476`）。点浮窗不抢炉石焦点。菜单「锁定窗口」保留为穿透开关，默认关。
   - SwiftUI 内容吞 mouseDown，拖拽靠 hosting view 的 `mouseDownCanMoveWindow`，折叠箭头等控件除外。
   - 吸附目标：炉石窗口四边 + 屏幕四边，阈值约 12 pt。
   - 持久化**锚点**（吸哪条边 + 沿边百分比偏移），不存绝对 rect —— 即 Phase 5 调研好的 HDT 方案（存百分比、横向按画面比例折算）。「自动定位」改成读锚点；吸右边、偏移 0 等于今天的默认位置，默认体验不变。计数器面板（Phase 5）之后照抄。
3. 三行头行高 40 → 21 会不会太挤，实机看。

## 不在本片

动效（T8）、已打出段状态图标（2.7）、计数器面板（Phase 5）、对手侧三行头重做。
